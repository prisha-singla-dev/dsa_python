
Loops-
- for loops- 
for i in range (0,x):
    print(i);

i in range (n-1,0,-1):
//range(start, stop, step)

//add elements in the another array
for i in range (0, d):
    temp.append(arr[i])


rotated_arr = arr[d:]+arr[:d]
//after d elements first and then before d elements

def union_sorted_array(arr1,arr2):
    # Brute Force Approach
    return sorted(set(arr1) | set(arr2))
// | - UNION OPERATOR
// set - stores unique elements


return sorted(set(arr1) & set(arr2))
// & - intersection 


    visited = [0]*m
this means 0* in m no. of elements


MAPS 
num_map = {}
    for i, num in enumerate(nums):
        complement = target - num
        if complement in num_map:
            return (num_map[complement], i)
        num_map[num] = i
    return None


enumerate() - It gives you index + value together while looping.
